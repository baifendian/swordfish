package com.baifendian.swordfish.common.enums;

/**
 * hive 字段类型
 */
public enum HiveColumnType {
  /**
   * 对应支持的hive字段类型
   */
  TINYINT, SMALLINT, INT, BIGINT, FLOAT, DOUBLE, STRING, VARCHAR, CHAR, BOOLEAN, DATE, TIMESTAMP;
}
